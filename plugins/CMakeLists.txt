# 协议插件目录。
#
# 这里每个子目录都会编成一个**独立动态库**，产物落到可执行文件旁的
# `plugins/protocols/`，由主程序的 QPluginLoader 加载 —— 主程序代码里
# 没有任何一个协议的名字。
#
# 于是"新增一种协议"= 往这里加一个插件、编一下，**核心代码零改动、零重编译**；
# 反过来，把某个插件的 .dll 删掉，主程序照样正常启动，只是设备对话框里少一个选项。

set(CMAKE_AUTOMOC ON)

add_subdirectory(http_json)
